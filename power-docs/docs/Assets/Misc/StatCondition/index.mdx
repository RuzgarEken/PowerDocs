---
sidebar_position: 1
---

import ConditionRef from './images/ConditionRef.png'
import ConditionSignatureRef from './images/ConditionSignatureRef.png'

# Stat Condition

A **Stat Condition** checks whether a Stat satisfies a specified condition.

It is used by condition-based Processors and Injectors to determine whether a particular operation should be executed.

For example

<img src={ConditionRef} alt="Condition reference" />
<img src={ConditionSignatureRef} alt="Condition usage reference" />

the **ChainHook CdDegenerator Trigger** shown below uses a Stat Condition to activate only when the **ChainHook Cooldown** is greater than **0**.